<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/public/css/style.css"/>
</head>
<body>
  <header>
    <nav>
      <a href="/">Home</a>
      <!-- Conditionally render Dashboard, Login, Logout links -->
      {{#if logged_in}}
        <a href="/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
      {{else}}
        <a href="/login">Login</a>
      {{/if}}
    </nav>
  </header>

  {{{body}}}

</body>
</html>
